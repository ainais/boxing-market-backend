spring:
  datasource:
    url: jdbc:h2:mem:boxingmarket;MODE=MySQL  # ??? DB, MySQL ??
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true  # ? ?????? DB ?? ???? ??
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create  # ??? ? ???? ?? ?? (???)
    show-sql: true      # ???? SQL? ??? ???
    properties:
      hibernate:
        format_sql: true